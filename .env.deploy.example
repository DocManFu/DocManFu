# Deployment target configuration
# Copy to .env.deploy and fill in your values

# SSH host (IP or hostname)
DEPLOY_HOST=192.168.1.100

# Path on the remote server
DEPLOY_PATH=/home/user/docmanfu

# Docker Compose profile (optional): ollama, ollama-cpu, or leave empty for none
DEPLOY_PROFILE=
